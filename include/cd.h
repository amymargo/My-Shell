#ifndef CD_H
#define CD_H

/**
 * The function of the cd command is to change the current working directory of the shell.
 * @param args  An array of strings representing the user input command and its arguments.
 * @return      Returns 0 if it succeeds, 1 if it fails.
 */
int cd(char **args);

#endif
